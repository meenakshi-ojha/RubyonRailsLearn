
<h1>Students in <%=@department.name%> </H1>
<ul>
  <% @department.students.each do |student| %>
    <li>
   
    <%=  link_to student.name, department_student_path(@department,student) %>
    
    </li>

  <% end %>
</ul>
<%= link_to "home", root_path %>
<%= link_to "New student", new_department_student_path(@department,@student) %>